<odoo>

    <record id="real_estate_list_view" model="ir.ui.view">
        <field name="name">real_estate.list</field>
        <field name="model">real_estate</field>
        <field name="arch" type="xml">
            <list decoration-muted="state == 'sold'"
                decoration-bf="state == 'offer_received'"
                decoration-success="state == 'offer_accepted'">
                <field name="name" />
                <field name="property_type" options="{'no_create': True}" />
                <field name="postcode" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="hide" />
            </list>
        </field>
    </record>

    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">real_estate_type.list</field>
        <field name="model">real_estate_type</field>
        <field name="arch" type="xml">
            <list default_order="sequence">
                <field name="sequence" widget="handle" />
                <field name="name" />
                <field name="offer_count" />
            </list>
        </field>
    </record>

    <record id="view_estate_property_tag_list" model="ir.ui.view">
        <field name="name">real_estate_tag.list</field>
        <field name="model">real_estate_tag</field>
        <field name="arch" type="xml">
            <list default_order="sequence" editable="bottom">
                <field name="sequence" widget="handle" />
                <field name="name" />
            </list>
        </field>
    </record>

    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">real_estate_offer.list</field>
        <field name="model">real_estate_offer</field>
        <field name="arch" type="xml">
            <list>
                <field name="price" />
                <field name="partner_id" />
                <field name="property_id" />
                <field name="status" />
                <field name="date_deadline" />
            </list>
        </field>
    </record>

</odoo>
<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="real_estate_form_view" model="ir.ui.view">
        <field name="name">real_estate.form</field>
        <field name="model">real_estate</field>
        <field name="arch" type="xml">
            <form string="Properties">
                <header>
                    <button name="action_set_sold" type="object" string="Sold" class="btn-primary"
                        invisible="state == 'sold'" />
                    <button name="action_set_cancel" type="object" string="Cancel"
                        class="btn-secondary"
                        invisible="state == 'sold'" />
                    <field name="state" widget="statusbar" />
                </header>
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="tag_ids" widget="many2many_tags"
                            options="{'color_field': 'color'}" />
                        <group>
                            <field name="property_type" options="{'no_create': True}" />
                            <field name="postcode" string="Postcode" />
                            <field name="date_availability" string="Available From" />
                        </group>
                        <group>
                            <field name="expected_price" string="Expected Price" />
                            <field name="best_offer" />
                            <field name="selling_price" string="Selling Price" readonly="1" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" string="Description" />
                                <field name="bedrooms" string="Bedrooms" />
                                <field name="living_area" string="Living Area (sqm)" />
                                <field name="facades" string="Facades" />
                                <field name="garage" string="Garage" />
                                <field name="garden" string="Garden" />
                                <field name="garden_area" string="Garden Area (sqm)"
                                    invisible="not garden" />
                                <field name="garden_orientation" string="Garden Orientation"
                                    invisible="not garden" />
                                <field name="total_area" string="Total Area (sqm)" />
                            </group>
                        </page>
                        <page
                            string="Other Info">
                            <group>
                                <field name="property_salesperson" string="Salesman" />
                                <field name="property_buyer" string="Buyer" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids">
                                <list editable="bottom" decoration-success="status == 'accepted'"
                                    decoration-danger="status == 'refused'">
                                    <field name="price" />
                                    <field name="partner_id" />
                                    <field name="validity" />
                                    <button name="action_accept" type="object" icon="fa-check"
                                        title="Aceptar oferta" invisible="status != 'pending'" />
                                    <button name="action_refuse" type="object" icon="fa-times"
                                        title="Rechazar oferta" invisible="status != 'pending'" />
                                    <field name="date_deadline" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>

        </field>
    </record>

    <record id="view_estate_property_type_form" model="ir.ui.view">
        <field name="name">real_estate_type.form</field>
        <field name="model">real_estate_type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <header>
                    <!-- Stat Button for Offers -->
                    <button name="action_view_offers"
                        type="object"
                        icon="fa-handshake-o"
                        string="Offers"
                        class="oe_stat_button">
                        <field name="offer_count" string=" Offers" widget="statinfo" />
                    </button>
                </header>
                <sheet>
                    <group>
                        <field name="name" />
                    </group>
                    <notebook>
                        <page string="Properties">
                            <field name="property_ids">
                                <list>
                                    <field name="name" />
                                    <field name="expected_price" />
                                    <field name="state" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

   <record id="view_res_users_form_inherit_with_properties" model="ir.ui.view">
            <field name="name">res.users.form.with.properties</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <!-- Agregar una nueva pestaña con el filtro aplicado -->
                <xpath expr="//sheet//notebook" position="inside">
                    <page string="Properties">
                        <!-- Filtro para solo mostrar propiedades disponibles (estado 'new') -->
                        <field name="property_ids"  domain="[]">
                            <list decoration-muted="state == 'sold'"
                                  decoration-bf="state == 'offer_received'"
                                  decoration-success="state == 'offer_accepted'">
                                <field name="name" />
                                <field name="property_type" options="{'no_create': True}" />
                                <field name="postcode" />
                                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="expected_price" />
                                <field name="selling_price" />
                                <field name="date_availability" />
                            </list>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>

</odoo>